// 配置项

const host = 'http://localhost:5757'
// const host = 'https://nxhlnolc.qcloud.la'
// const host = 'https://619092715.chengzimini.xyz'
const config = {
  host,
  loginUrl: `${host}/weapp/login`,
  userUrl: `${host}/weapp/user`
}

export default config
